import { CoreOptions } from "@/commands/core";

export class DbSchemaCommandOption extends CoreOptions {

  constructor() {
    super();
  }
  
  toString(): string {
    return `${super.toString()}`;
  }
} 